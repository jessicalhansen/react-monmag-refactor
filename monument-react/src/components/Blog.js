import React from 'react';

const Blog = () => {
    return (
        <div>
            <h1>Blog</h1>
            {/* replace h1 with components for blog, don't forget styling. */}
        </div>
    )
};

export default Blog;
